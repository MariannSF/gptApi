A Flask-based web application that allows users to query a movie dataset using natural language.
The app integrates OpenAI to translate user input into SQL queries executed on a SQLite database built from Kaggle data. 
It features a dual interface with both a structured data query engine and a conversational chatbot, highlighting the difference between database-driven results and AI-generated responses.
